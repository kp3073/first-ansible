- name: install nginx
  hosts: 172.31.30.32
  become: true

  tasks:
    - name: install nginx
      ansible.builtin.dnf:
        state: latest

    - name: start nginx
      ansible.builtin.systemd:
         state: started
         name: nginx

    - name: update repository index
      dnf:
        update_cache: yes